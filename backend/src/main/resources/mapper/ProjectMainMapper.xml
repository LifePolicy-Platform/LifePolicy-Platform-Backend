<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="maventest.visit.mapper.ProjectMainMapper">

    <select id="selectActiveProjects" resultType="maventest.customer.dto.ActiveProjectOptionDto">
        SELECT CAMP_CODE       AS campCode,
               CAMP_NAME       AS campName,
               CAMP_SERVICE_DT AS campServiceDt
        FROM tb_camp_mst
        WHERE CAMP_SERVICE_DT <![CDATA[ >= ]]> CURDATE()
        ORDER BY CAMP_SERVICE_DT ASC, CAMP_NAME ASC
    </select>

</mapper>
